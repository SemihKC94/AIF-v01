using UnityEngine;
using TMPro;

namespace SKC.AIF.Pool
{
    [CreateAssetMenu(menuName = nameof(AIF) + "/" + nameof(UI) + "/" + nameof(TextPool))]
    public class TextPool : Pool<TextMeshPro>
    {

    }
}
