using UnityEngine;
using SKC.AIF.Tools;

namespace SKC.AIF.Data
{
	[CreateAssetMenu(menuName = nameof(AIF) + "/" + nameof(Data) + "/" + nameof(GatheringToolDefinitionLookup))]
	public class GatheringToolDefinitionLookup : IndexLookup<GatheringToolDefinition>
	{

	}
}
