using SKC.AIF.Tools;
using UnityEngine;

namespace SKC.AIF.Data
{
	[CreateAssetMenu(menuName = nameof(AIF) + "/" + nameof(Data) + "/" + nameof(Data) + "/" + nameof(GatheringToolDatabase))]
	public class GatheringToolDatabase : ObjectDatabase<GatheringToolDefinition>
	{

	}
}
